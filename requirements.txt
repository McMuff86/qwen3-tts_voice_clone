# Qwen3-TTS Voice Cloning Requirements
# =====================================
# Install with: pip install -r requirements.txt
# Or with uv:   uv pip install -r requirements.txt

# Core TTS
qwen-tts

# PyTorch (CUDA 12.1) - adjust for your CUDA version
--extra-index-url https://download.pytorch.org/whl/cu121
torch
torchaudio

# Pinned versions (qwen-tts compatibility)
transformers==4.57.3
huggingface-hub<1.0

# Audio processing
soundfile
numpy

# Web UI
gradio

# REST API
fastapi
uvicorn[standard]

# Jupyter Notebook support
ipykernel
ipywidgets

# Optional: for recorder.py (currently not needed, uses WAV)
# pydub

# Optional: Whisper transcription (for auto-transcript feature)
# openai-whisper is more stable on Windows
openai-whisper
# faster-whisper  # Alternative, faster but has ctranslate2 issues on Windows

# Optional: Flash Attention (requires CUDA 12.4+)
# See: https://huggingface.co/lldacing/flash-attention-windows-wheel
# flash-attn
