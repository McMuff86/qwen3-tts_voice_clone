# Qwen3-TTS Voice Clone – Environment Configuration
# Copy to .env and adjust as needed: cp .env.example .env

# ─── Device & Performance ─────────────────────────────────────────────────────
# CUDA device (e.g. cuda:0, cuda:1, cpu)
QWEN_TTS_DEVICE=cuda:0

# Precision: bfloat16 (recommended), float16, float32
QWEN_TTS_DTYPE=bfloat16

# Attention implementation: sdpa (default, no extra deps) or flash_attention_2
QWEN_TTS_ATTN=sdpa

# ─── Model ────────────────────────────────────────────────────────────────────
# Model size: 1.7B (better quality) or 0.6B (faster, less VRAM)
QWEN_TTS_MODEL_SIZE=1.7B

# ─── Defaults ─────────────────────────────────────────────────────────────────
# Default generation language
# Supported: Chinese, English, Japanese, Korean, German, French, Russian,
#            Portuguese, Spanish, Italian
QWEN_TTS_LANGUAGE=German

# ─── Paths ────────────────────────────────────────────────────────────────────
# Voice reference files directory
# QWEN_TTS_VOICES_DIR=assets/voices

# Generated output directory
# QWEN_TTS_OUTPUT_DIR=assets/output

# ─── Gradio Web UI ────────────────────────────────────────────────────────────
# Server port for the web interface
QWEN_TTS_GRADIO_PORT=7861
